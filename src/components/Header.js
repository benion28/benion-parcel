const Header = () => {
    const template = `
        <header>
            <h1>Benion Parcel App</h1>
            <p>This is a boilerplate for simple JS workflow using Parcel</p>
        </header>
    `;
    return template;
}

export default Header;